// Script básico para manejar el login y registro
document.getElementById('loginForm')?.addEventListener('submit', function(e) {
    e.preventDefault();
    // Aquí agregar la lógica de inicio de sesión
    alert('Inicio de sesión exitoso!');
});

document.getElementById('registerForm')?.addEventListener('submit', function(e) {
    e.preventDefault();
    // Aquí agregar la lógica de registro
    alert('Registro exitoso!');
});

document.getElementById('sendMoneyForm')?.addEventListener('submit', function(e) {
    e.preventDefault();
    // Aquí agregar la lógica para enviar dinero
    alert('Dinero enviado!');
});
